## Packages
framer-motion | Smooth animations for list items and status changes
date-fns | Formatting timestamps for the sync history log

## Notes
The dashboard simulates sync operations.
The "Sync" button triggers a POST request.
The history list should auto-refresh or update optimistically to show "pending" then "success".
Color palette should be technical, dark mode, "developer tool" aesthetic.
